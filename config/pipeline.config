# Pipeline promotion order
# Defines the required sequence for feature branches before they can be merged to each target.
# Each branch must be merged to all preceding branches before proceeding to the next.
#
# Example: stage,prod means a feature branch must be merged to stage before it can go to prod.
PROMOTION_ORDER=dev,stage,prod
